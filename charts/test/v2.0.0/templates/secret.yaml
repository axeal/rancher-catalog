apiVersion: v1
data:
  {{- range $key, $val := .Values.secretContents }}
  {{ $key }}: {{ $val | b64enc | quote }}
  {{- end }}
kind: Secret
metadata:
  name: mysecret
  namespace: default
type: Opaque
